# Dependencies

- Runtime: Python 3.9 or newer, standard library only.
- Test runner: Python `unittest`, included in the standard library.
- Optional package build: `setuptools>=68` as declared in `pyproject.toml`; no build is required for the demo commands.
- Network: not required for the bundled replay, workflow demo, or tests.
