# Codex use evidence

## Evidence included without private history

- Automation identifier: `adam-studio`.
- Build sequence task cards: steps 2–6, dated 2026-07-17.
- Current submission-preparation heartbeat: `2026-07-17T14:42:22.446Z`.
- Reproducible artifacts: versioned schemas, golden replay, regression tests, terminal workflow demo, and completion receipts retained outside this public package.

## IDs not guessed

Codex task, session, and run identifiers are not reliably available inside the filesystem and are therefore marked `NOT_AVAILABLE` here. If the submission form requires them, copy the visible identifiers from the Codex task UI immediately before submission. Do not search unrelated task history or expose private conversations.

## Suggested proof capture

1. Screenshot the current Codex task header with the task title and visible timestamp/ID.
2. Screenshot the terminal output from `python3 demo_v0_1.py --tests`.
3. If allowed, include a redacted screenshot of the task card and completion receipt; exclude local paths and private task text.
