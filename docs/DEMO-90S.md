# 60–90 second demo

## Run command

```bash
python3 demo_v0_1.py
```

## Script and shot list

| Time | Shot | Narration |
|---|---|---|
| 0–10s | Title and README one-line summary | “Handmade Creator Radar turns a natural-language brief into an auditable cross-platform evidence decision.” |
| 10–25s | Show task schema, filter tests, and the combined making-plus-doll rule | “The filter is deliberately narrow: generic craft content does not enter accepted.” |
| 25–45s | Run `python3 demo_v0_1.py` and highlight `ready`, `replay_golden`, and decision counts | “The first demo is offline and reproducible. Replay evidence is labeled honestly and never mixed with live evidence.” |
| 45–60s | Highlight both platform tiers as `insufficient_evidence` | “The system refuses to turn a single viral post or missing values into a platform conclusion.” |
| 60–75s | Run `python3 demo_v0_1.py --workflow` | “Success, human confirmation and resume, and failure all reach explicit terminal states.” |
| 75–90s | Run `python3 demo_v0_1.py --tests`; end on limitations | “The full offline suite passes. V0.1 supports two platforms and one observation window, so long-term trend remains future work.” |

## Recording checklist

- Use a clean terminal at readable scale.
- Show only this submission package.
- Keep personal paths and account UI outside the frame.
- Do not imply replay data is live.
- Keep final video under the event limit.
